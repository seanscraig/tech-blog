{{!-- <!-- Loop over each object in the projects array -->
{{#each projects as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}

{{#each posts as |post|}}
<div class="card">
  <div class="card-header">
    {{user.user_name}} at {{format_date post.created_at}}
  </div>
  <div class="card-body">
    <h5 class="card-title">{{post.title}}</h5>
    <p class="card-text">{{post.body}}</p>
  </div>
</div>
{{/each}}



{{!-- {{#each posts as |post|}}
  <div class="row align-center mb-5 card">
    <div class="col-12 col-md-6 col-lg-12 card-header">
      <h2>{{post.title}}
        by:
        {{user.user_name}}
        at
        {{format_date post.created_at}}</h2>
      <div class="card-body">
        <p>{{post.body}}</p>
      </div>
    </div> --}}
    {{!-- <div class="col-12 col-md-6 col-lg-12">
    {{#each comments as |comment|}}
    <p>{{comment.body}} id: {{comment.user_id}} name: {{user.user_name}}</p>
    {{/each}}
  </div> --}}
  {{!-- </div>
{{/each}} --}}