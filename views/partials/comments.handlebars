<div class="comments">
    <div class="card">
      <div class="card-header">
        <h3>Comments</h3>
      </div>
      {{#each post.comments as |comment|}}
      <div class="card-body">
        <p>{{body}}</p>
        <p>-{{user.user_name}}, {{format_date created_at}}</p>
      </div>
    </div>
  {{/each}}
</div>