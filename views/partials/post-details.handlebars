<div class="card">
  <div class="card-header flex-row justify-space-between align-center">
    <h2>
      <a href="/post/{{ id }}">{{ title }}</a>
    </h2>
    <h5>{{ user.user_name }} on {{format_date created_at}}</h5>
  </div>
  <div class="card-body">
    <p class="card-text">{{ body }}</p>
  </div>
</div>
